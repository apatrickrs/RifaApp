<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rifa.mrrifa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ProcurarNomeActivity"></activity>
        <activity android:name=".adapter.AdapterNome" />
        <activity
            android:name=".activity.ColocarNomeActivity"
            android:screenOrientation="locked" />
        <activity
            android:name=".activity.VerNomesActivity"
            android:screenOrientation="locked" />
        <activity
            android:name=".activity.OpcoesActivity"
            android:screenOrientation="locked">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>